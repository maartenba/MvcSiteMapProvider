#if MVC2
using System;

namespace MvcSiteMapProvider.Web.Mvc.Filters
{
    /// <summary>
    /// Dummy class used to satisfy the IGlobalFilterProvider contract in MVC2, which does not support filters.
    /// </summary>
    public class Filter
    {
    }
}
#endif
